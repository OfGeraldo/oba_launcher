// src/utils/missedCalls.ts
export const fetchMissedCallsToday = async (): Promise<number> => {
  // MOCK: aqui você integra com módulo nativo depois.
  // Por enquanto retorna sempre 0.
  return 0;
};
